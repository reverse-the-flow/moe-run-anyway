# Expert Paging Step Breakdown

This breaks the roadmap into small steps so blockers are visible early. A step
should not advance unless its evidence is written as an artifact, fixture,
manifest, report, or test output.

## Status Labels

- `done`: implemented and validated in the repository.
- `active`: current work can proceed without changing the roadmap.
- `blocked`: the next action needs a model, runtime, machine, credential, or
  upstream patch that is not currently available.
- `planned`: valid next work, but another step should land first.

## Phase 0: Harness Contract And Model Plane Handoff

| Step | Status | Evidence | Blocker |
| --- | --- | --- | --- |
| Define the Model Plane manifest handoff. | done | Saved manifest fixtures and manifest planner. | None. |
| Define managed expert loading vocabulary. | done | Managed loading contract and machine-readable plan. | None. |
| Add no-side-effect validation commands. | done | Planner scripts validate without downloads, secrets, Docker, servers, or prompt traffic. | None. |
| Separate runtime evidence from semantic routing evidence. | done | Roadmap truth table and artifact contracts. | None. |

## Phase 1: Runtime Baseline Evidence

| Step | Status | Evidence | Blocker |
| --- | --- | --- | --- |
| Validate runtime baseline artifact shape. | active | `scripts/plan_runtime_baseline_artifacts.py`. | None. |
| Confirm passive sidecar implementation. | done | `llama_sidecar.py` forwards traffic, captures request/response summaries, optional upstream observability, and shared-contract artifacts. | None. |
| Capture a passive sidecar artifact bundle. | active | Mixtral sidecar smoke bundle captured; expected bundle shape remains sidecar `manifest.json`, `events.jsonl`, `summary.json`, upstream observability snapshots when available, and Model Plane run metadata. | Needs repeated sidecar traffic for each selected backend if comparing sidecar behavior. |
| Capture a llama.cpp baseline artifact bundle. | active | Mixtral and Qwen3 GGUF baseline bundles captured; Nemotron Super GGUF failed at llama.cpp load due tensor-shape compatibility. | Needs no new prompt traffic for Nemotron Super until runtime compatibility is inspected. |
| Capture a vLLM or OpenAI-compatible baseline artifact bundle. | active | Nemotron Omni NVFP4 vLLM readiness and one-prompt baselines captured, including a 512-token-cap rerun with `finish_reason=stop`. | Needs repeated prompt-family coverage if comparing behavior, not just runtime compatibility. |
| Compare backend observability fields. | active | `docs/calliope-moe-architecture-pass.md` records first backend differences. | Needs a compact comparison report across Mixtral, Qwen3, and Nemotron Omni artifacts. |
| Produce a Phase 1 blocker report. | planned | Expected: missing capability list for inventory, routing visibility, residency read/write, fallback, cleanup, and artifact export. | Needs baseline artifacts. |

## Phase 1B: Offline Expert Store Preparation

This track is added because live paging needs a processed expert store, but that
store should not require loading the full model into RAM.

| Step | Status | Evidence | Blocker |
| --- | --- | --- | --- |
| Review expert-paging inspiration for verified build-up. | done | `docs/flash-moe-inspiration-next-step.md` records architecture lessons from Flash-MoE: expert index first, dry-run packing, replay metrics, and rejected cache/prefetch ideas. | None. |
| Define an expert inventory manifest schema. | done | `data/expert_inventory_manifest.fixture.json` and `scripts/plan_expert_inventory.py` validate layer/expert/component rows, tensor names, source shard, byte offsets, dtype, shape, estimated bytes, coverage status, missing components, duplicates, overlaps, and totals. | None for the fixture schema; real scanners still need checkpoint files. |
| Build a safetensors shard scanner. | done | `scripts/scan_safetensors_expert_inventory.py` reads safetensors headers and optional shard indexes, emits the expert inventory schema, and validates scanner output without loading tensor values. Synthetic tests create temporary shards and cover indexed scans, missing components, output writes, and invalid headers. | Needs a real local or cloud-accessible safetensors checkpoint to produce real-model inventory evidence. |
| Build a GGUF tensor-table scanner. | done | `scripts/scan_gguf_expert_inventory.py` reads GGUF metadata and tensor tables, supports per-expert tensors and stacked `*_exps` tensors, splits stacked tensors into per-expert inventory rows, and validates output without loading tensor values. Synthetic tests create temporary GGUF files and cover stacked tensors, per-expert tensors, missing expert-count metadata, missing components, output writes, and invalid files. | Needs a real local GGUF inventory run against a traced model before claiming real-model byte evidence. |
| Build a streaming expert packer dry run. | done | `scripts/plan_expert_store_layout.py` plans packed expert target files, component offsets, source read ranges, required disk bytes, missing components, and write-path status without reading tensor values or writing a store. | None for fixture-level layout; real-model layout needs scanner-derived inventory. |
| Build a streaming expert packer write path. | planned | Expected: writes packed expert files and manifest by streaming tensor data. | Needs enough disk and a target checkpoint. |
| Validate partial-shard behavior. | planned | Expected: partial inventory report says exactly which experts/tensors are missing. | Needs intentionally incomplete shard set. |
| Validate full-store consistency. | blocked | Expected: sampled tensor hashes or byte ranges match source checkpoint. | Needs complete checkpoint files, but not full RAM load. |

Partial shards are useful for inventory and failure-mode testing. They are not
valid evidence for learned routing policy.

## Phase 2: Semantic Routing Traces

This phase has validated direct GGUF traces and remains open for the Python
hookable-runtime lane. Passive sidecar evidence stays useful, but semantic
routing should come from hookable internals before any fork, controller, or live
actuator work.

| Step | Status | Evidence | Blocker |
| --- | --- | --- | --- |
| Confirm synthetic hook smoke path. | done | `run_forward_probe_demo.py` exercises hook attach, router event capture, spans, and window summaries without a model. | None. |
| Classify current PC/GX10 hook candidates. | done | `data/hookable_moe_attempt_matrix.json` records PC HF non-MoEs, PC/GX10 GGUF engine-hook candidates, and the GX10 Nemotron HF dependency blocker. | None. |
| Select a hookable MoE target. | done | GX10 Mixtral/Qwen3 and six PC Ollama GGUF blobs produced llama.cpp engine-hook traces; GX10 Nemotron HF remains a Python hook candidate blocked by missing trusted-code dependencies. | Python track still needs a compatible Transformers MoE runtime. |
| Build a thin Python target runner around `ForwardHookMoEProbe`. | planned | Expected: runner loads one user-provided local model, attaches hooks, runs prompt cases, writes existing probe artifacts, and does not create a new artifact shape. | Needs compatible Transformers target/runtime. |
| Build a minimal llama.cpp router trace patch. | done | `patches/llama-cpp-moe-router-trace-example.patch` adds an eval-callback trace example; GX10 and PC runs emitted selected expert ids and weights. | None for direct GGUF hook smoke. |
| Capture router outputs with hooks. | active | GX10 Mixtral: 96 events. GX10 Qwen3: 144 events. PC traces now include Mixtral, Nemotron Cascade, Qwen3 Coder A3B, Gemma4 26B A4B, Nemotron Nano A3B, and Qwen3 Coder 30B alias. | Needs Python-track target and repeatable launch-card packaging. |
| Define the repeat gate for this phase. | done | `docs/phase-2-repeat-protocol.md` separates matrix validation, GGUF metadata preflight, and full semantic trace acceptance criteria. | Fresh PC Mixtral full-trace repeat exceeded a 15-minute guardrail with empty JSONL, so rerun needs a longer or cleaner window. |
| Preflight oversized GGUF MoE targets. | done | PC Llama 4 Scout: 48 routed layers, 16 experts, top-k 1. PC Qwen3 Coder Next: 48 routed layers, 512 experts, top-k 10. PC DeepSeek V3.1: 58 routed layers, 256 experts, 1 shared expert, top-k 8. | Full router traces need a higher-memory host or Docker memory increase. |
| Capture dense or full-runtime fallback output. | planned | Expected: baseline outputs for the same prompt set. | Needs runnable target. |
| Validate trace artifacts against the shared contract. | planned | Expected: trace validation report. | Needs trace artifacts. |
| Record hook failure modes. | planned | Expected: failures are explicit and do not downgrade to timing inference. | Needs hook attempts. |

If local hardware cannot run the target, cloud is the clean route for this
phase. Running with fewer shards can test error handling, but it cannot produce
trustworthy routing traces.

## Phase 3: Inventory-Grounded Replay Audit

This is the current active audit lane. It now has six repo-local real
trace/inventory/replay bundles and six local handoff scaffolds, while policy-candidate readiness, fallback
comparison, and live actuator proof remain open blockers.

| Step | Status | Evidence | Blocker |
| --- | --- | --- | --- |
| Add expert inventory fixture and validator. | done | `scripts/plan_expert_inventory.py` validates the fixture and emits Phase 3 replay join keys plus estimated residency bytes. | None for fixture validation. |
| Join semantic traces to inventory. | done | `scripts/plan_trace_inventory_replay.py` joins the llama.cpp router trace fixture to the expert inventory fixture, reporting route count, joined routes, unique experts, estimated bytes, reuse-distance facts, per-layer byte summaries, missing routes, and baseline policy prerequisites. | None for fixture-level join; real-model evidence still needs scanner output from a traced model. |
| Audit real-model trace/inventory pairing. | done | `scripts/plan_real_model_trace_inventory_pairing.py` validates model, backend, contract, component coverage, fixture signals, and joined route counts; default fixture output is valid schema evidence but reports `fixture_only_trace_inventory_pair`, while the repo-local Phase 3 matrix now reports six scanner-derived real-model pairs ready. | None for the current repo-local real bundles; new models still need same-model scanner inventory before claiming real-model evidence. |
| Define baseline policies. | done | `memory-moe-mvp/data/baseline_replay_policies.json` and `scripts/plan_baseline_replay_policies.py` define and validate observe-only, keep-hot, preload-shortlist, evict-cold, fallback-dense, and no-live-actuator policies, required metrics, allowed managed-loading actions, non-mutating replay mode, and rejection reasons. | None for policy definitions. |
| Replay policies over trace+inventory artifacts. | done | `scripts/plan_baseline_policy_replay.py` simulates observe-only, keep-hot, preload-shortlist, evict-cold, fallback-dense, and no-live-actuator over joined trace+inventory artifacts; reports miss rate, warm-hit rate, churn, fallback frequency, byte misses, candidate policies, policy-candidate diagnostics, and remaining Phase 3 blockers without runtime mutation. | None for fixture-level replay; current real bundles still have no policy candidate because they have no reuse-distance observations, and dense fallback quality bounds plus live residency controls remain missing. |
| Build a Phase 3 prompt-set artifact. | done | `scripts/build_phase3_prompt_set.py` builds shared repeated prompt sets; all six real bundles now have prompt-set artifacts that policy-candidate and dense fallback planners can consume. | None for prompt metadata; runtime trace capture still needs explicit approval. |
| Build Phase 3 output-summary templates. | done | `scripts/build_phase3_output_summary.py` builds and validates prompt-set-backed managed and dense summary templates with capture receipts; all six real bundles now have managed and dense templates under their `*-fallback/` folders. | Templates cover every prompt id, but actual output rows still need approved runtime captures and ready receipts before fallback comparison can advance. |
| Build Phase 3 runtime-capture request. | done | `scripts/build_phase3_runtime_capture_request.py` binds each real bundle prompt set, candidate router trace path, trace receipt template, managed/dense output-summary fills, live-proof template fill, approvals, future-adapter status, and validator commands into per-bundle runtime-capture requests. | Requests are valid and not complete; runtime prompt traffic and artifact capture remain approval-bound. |
| Audit saved Phase 3 runtime-capture requests. | done | `scripts/plan_phase3_runtime_capture_request.py` reloads all six saved requests, rebuilds expected request state from current local metadata, reports zero path/status/approval/embedded-summary/source/validator-command drift, confirms required validator-command coverage for all saved requests, recommends the first approval-bound capture candidate with queue rank and selection rationale, lists the missing approval flags, emits an approval metadata rebuild command plus a compact approval command manifest for recording approved booleans, emits a metadata-only post-approval preview proving the recommended request becomes ready-for-operator without capture completion or mutation, emits an ordered approve/capture/fill/intake sequence, renders capture receipt requirements, emits a capture-queue selection summary with approval-required, ready-for-operator-capture, and approved-but-incomplete counts, and can write a pre-capture operator handoff report with `--output-md`. | Mixtral is currently rank 1 because it is the known sparse baseline; the sequence remains approval-required because runtime prompt traffic, router trace, managed output, and dense output approvals plus artifacts are intentionally absent. |
| Build Phase 3 runtime-capture command contract. | done | `scripts/plan_phase3_runtime_capture_commands.py` exports a planned-only launch-card template and validates filled cards with `--launch-card`; `scripts/plan_phase3_evidence_packet.py --output-runtime-capture-launch-card` exports the recommended card, `--runtime-capture-launch-card` consumes one filled binding, and `--runtime-capture-launch-card-dir` consumes a repo-wide filled-card directory to update all-request execution coverage without executing runtimes, Docker, endpoints, tokens, or prompt traffic. | Filled cards still require Model Plane real callable ids or launch commands, explicit approval gates, prompt-traffic acknowledgement, the exact runtime-capture request path, and the exact prompt-set binding; capture artifacts and receipts remain unfilled until approved runtime work happens. |
| Index Phase 3 launch-card library. | done | `scripts/plan_phase3_launch_card_library.py` summarizes all six planned-only runtime-capture launch cards, proving the library is template-ready and the binding handoff is ready with 18 exact task rows carrying prompt-set/artifact/receipt/request paths plus prompt-traffic acknowledgement, can write a fillable worksheet with `--output-binding-worksheet`, write a metadata-only Model Plane artifact-writer contract request with `--output-model-plane-contract-request`, refresh the default saved worksheet and contract request with `--write-default-handoff-artifacts`, audit those saved handoff artifacts for missing files or drift, validate a filled worksheet with `--binding-worksheet`, validate a returned Model Plane fulfillment with `--model-plane-contract-fulfillment`, and emit offline-validated filled cards with `--output-filled-card-dir`, while showing zero Model Plane binding-ready cards, zero runtime-command-ready cards, and 18 unbound runtime command slots. | Library execution remains blocked until Model Plane callable ids or launch commands are filled after explicit approval and bindings preserve the exact request/artifact/receipt paths. |
| Audit Phase 3 capture-result intake. | done | `scripts/plan_phase3_capture_result_intake.py` reloads saved runtime-capture requests after local artifacts are filled, requires each saved request to be drift-free, reports aggregate drift-free/drifted saved-request counts, ready-for-operator and approved-but-incomplete request counts, runtime-approval-missing versus approved-runtime-capture-pending counts, aggregate capture-receipt gate coverage, flat receipt-fill manifest readiness split by candidate-router trace, managed-output, and dense-output artifact classes, output receipt binding readiness, reports metadata-only approval rebuild command availability plus a compact approval command manifest for missing-approval runtime steps, emits a metadata-only approval transition preview showing which request becomes ready-for-operator while receipt fills remain missing, emits a post-approval capture-fill plan with artifact/receipt paths, after-approval runtime statuses, missing-fill counts, and validator-command coverage, and verifies candidate trace receipts plus managed/dense output receipts are bound to the same saved request and prompt set with recorded approvals, reuses the policy-candidate trace, dense fallback, live-proof, and bundle validators, separates bundle-update readiness from Phase 4/live-spike readiness, reports the next operator step for each request, and can print it with `--show-queue` or write a Markdown report with detailed per-request queues and validator/build command blocks via `--output-md`. | Current requests are valid but capture-incomplete: no bundle is ready to update, no Phase 4 candidate is ready, and live proof remains absent. |
| Build Phase 3 live-capability proof template. | done | `scripts/build_phase3_live_capability_proof_template.py` creates `pc_mixtral_phase3_real_evidence_bundle.live-capability-proof.template.json`, a valid-but-not-ready artifact for residency observation, residency control, cleanup/restore, artifact export, source bundle, and prompt context. | Template is intentionally not proof; approved runtime evidence must fill it before live-spike readiness, and a ready proof must keep an existing source-bundle path plus existing exported artifact paths. |
| Plan Phase 3 runtime-actuator spike. | done | `scripts/plan_phase3_runtime_actuator_spike.py` converts the managed-loading backend gaps into proof requirements for expert inventory, routing visibility, residency observation, policy application, dense fallback, artifact export, residency control, and cleanup/restore, with patch/probe boundaries, proof artifacts, dependency edges, and completion gates. | This is a metadata-only spike handoff; live mutation remains blocked until the proof artifacts validate and explicit runtime approval exists. |
| Validate Phase 3 handoff scaffold coverage. | done | `scripts/plan_phase3_handoff_coverage.py` confirms all six real bundles have prompt sets, candidate-trace-receipt templates, managed/dense output-summary templates, runtime-capture requests, planned-only runtime-capture launch-card templates, and live-proof templates, and can write a Markdown coverage report with missing scaffold counts plus repair commands. | Runtime requests are not capture-complete; approved prompt traffic and live adapter proof are still required. |
| Compare against dense or full-runtime fallback. | done | `scripts/plan_dense_fallback_comparison.py` validates an optional comparison artifact, gates readiness on receipt-ready pair-consistent builder provenance, or records `fallback_output_missing_for_quality_comparison`; `scripts/build_dense_fallback_comparison.py` can build that artifact from receipt-ready managed and dense output summaries paired to the same request/prompt/model context or emit a valid not-ready template; `scripts/plan_phase3_dense_fallback_capture.py` turns a real bundle into prompt-set, saved-output, receipt, comparison-artifact, and updated-bundle requests and can write a Markdown handoff with blockers, runtime-capture contract, and commands via `--output-md`. | All six prompt sets and output-summary templates are scaffold-ready; actual quality bounds still need approved managed/dense outputs, ready capture receipts with existing source request and prompt-set bindings, and human review for non-exact quality labels. |
| Select go/no-go for live spike. | done | `scripts/plan_phase3_go_no_go.py` aggregates replay, the repo-local real-evidence matrix, handoff scaffold coverage, launch-card library and saved bridge artifact drift audit, runtime-capture request audit, policy-candidate trace receipt readiness, capture-result intake receipt gate and receipt-fill manifests, dense fallback comparison, managed-loading capability gaps, and runtime-actuator spike proof handoff into an explicit `no_go_live_spike` decision with reasons, policy-candidate blocked-bundle/no-reuse-distance/prompt-identity counts, launch-card template/binding/runtime-command and saved-handoff-artifact counts, actuator-spike proof counts, and the next candidate-trace operator step, and can write a durable Markdown decision report via `--output-md`. | None for the offline decision packet; current evidence remains no-go until receipt-bound capture artifacts, a policy-candidate replay, fallback quality bounds, residency control, and cleanup proof exist. |
| Assemble Phase 3 evidence packet. | done | `scripts/plan_phase3_evidence_packet.py` indexes inventory, layout, trace join, real-model pairing, policy definitions, replay, fallback comparison, optional live capability proof, repo-local matrix, matrix-aware pairing status, scaffold coverage, saved-request audit, runtime-capture queue selection, approval metadata rebuild command manifest, approval-manifest parity, receipt-requirement parity, path-aware operator-queue parity, recommended and all-request post-approval capture-fill parity, all-request capture queue manifest, all-request approval command manifest, all-request validator command manifest, all-request downstream handoff manifest, all-request receipt-fill manifest with candidate-router trace, managed-output, and dense-output class counts, all-request receipt-fill command manifest, all-request receipt/validator parity, receipt-command validator coverage, validator-manifest runtime/future coverage, Phase 3 blocker closure manifest with mapping-ready/evidence-complete status, Phase 3 blocker evidence ledger with row-level missing-evidence counts, Phase 3 blocker resolution queue with ordered work packages, next-unblocked work-package pointer, dependency order, completion gates, and closure-level validator counts, capture-result intake drift, receipt-gate counts, receipt-fill manifest counts, and approval transition preview counts, post-approval capture-fill plan counts, policy-candidate blocker evidence, matrix-aware go/no-go decision, an ordered promotion checklist, the recommended runtime-capture sequence, the recommended post-approval preview, the recommended runtime-capture preflight manifest, the recommended runtime-capture command contract, the repo-level launch-card library and binding handoff status, the recommended and all-request runtime-capture runtime-command/manual execution coverage, the all-request manual capture runbook, the all-request post-capture intake runbook, the recommended post-approval capture-fill plan, the recommended policy-candidate trace handoff, the recommended dense fallback capture handoff, the recommended live capability proof handoff, runtime-actuator design readiness, runtime-actuator spike proof handoff, the recommended and all-request validator command manifests, the all-request downstream handoff manifest, the all-request receipt-fill manifest, the all-request receipt-fill command manifest, the Phase 3 blocker closure manifest with mapping-ready/evidence-complete ready-scope status, Phase 3 blocker evidence ledger with row-level missing-evidence counts, and the all-request receipt/validator parity check into one reproducible audit packet; `--output-md` writes a Markdown report, `--output-operator-handoff-dir` writes a metadata-only handoff package with the evidence packet, README, launch-card binding worksheet, Model Plane artifact-writer contract request, recommended planned launch-card template, recommended runtime-capture preflight, recommended runtime-capture command contract, recommended runtime-capture execution coverage, all-request runtime-capture execution coverage, runtime-capture launch-card directory manifest, reuse-evidence capture plan, manual capture runbook, post-capture intake runbook, capture queue, approval command manifest, validator manifests, downstream handoff manifest, receipt-fill manifests with artifact-class counts, blocker-closure manifest, blocker-evidence ledger, and blocker-resolution queue; `scripts/plan_phase3_operator_handoff.py` validates that package offline, including recommended and all-request approval-command readiness, approval-command-manifest/capture-queue parity, recommended capture-queue agreement, recommended launch-card worksheet/template/capture-queue task parity, Model Plane artifact-writer contract request/schema/task parity, selected preflight packet/file parity, selected command-contract packet/file parity, selected execution-coverage packet/file parity, all-request execution-coverage packet/file parity, runtime-capture launch-card directory packet/execution/handoff parity with external staging directories allowed but repo-relative request and source-card paths checked, reuse-evidence capture-plan packet/gap/handoff/recommended-capture parity, manual-capture runbook packet/file parity, post-capture intake runbook packet/file parity, post-capture artifact-gate/manual-task/receipt-command parity, manual-task/receipt-command parity, manual-task source-request/prompt-set/approval-contract counter parity, all-request binding-worksheet/capture-queue task parity, repo-relative artifact/request/receipt path safety, and receipt-command/capture-queue artifact-key parity, receipt-fill artifact-class count parity, receipt-command validator coverage against capture-queue validator counts plus source-request/prompt-set/approval-contract readiness, validator-manifest runtime/future coverage, downstream handoff request/section/path coverage, blocker-closure reason/action/path coverage, blocker-evidence ledger row/count/packet parity, blocker-resolution queue package/row/dependency/completion-gate/validator parity, next-unblocked work-package pointer, plus runtime-actuator proof-handoff counts, requirement coverage, and control-dependency checks, `--policy-candidate-trace-receipt-path` attaches the ready candidate trace receipt required for Phase 3 completion, and `--live-proof-artifact-path` attaches residency/control proof when available. | Packet is ready for audit and now shows both the current promotion order, the blocker-resolution queue work-package order, dependency edges, completion gates, embedded runtime-actuator proof handoff, runtime-actuator design readiness, runtime-actuator spike handoff, and the Mixtral approve/capture/fill/intake sequence with a metadata-only post-approval preview, selected-request preflight, a planned-only command contract that names Model Plane/launch-card binding requirements and can export a planned-only launch-card template, selected and all-request execution coverage plus the manual capture runbook plus post-capture intake runbook showing manual capture and receipt intake are classified, approval metadata has a command, runtime capture command bindings are still missing, blocker-closure ready scope is mapping_ready_only rather than evidence-complete, and post-approval capture-fill plan; runtime capture remains approval-required until real artifacts are filled. |
| Plan real-evidence capture. | done | `scripts/plan_phase3_real_evidence_capture.py` turns packet gaps into concrete artifact requests, carries the policy-candidate trace receipt into bundle-builder commands, and can write a Markdown capture handoff with request statuses, planned outputs, validators, blocking requests, and safety contract; `scripts/plan_phase3_artifact_intake.py` scans generated router traces, matches optional GGUF inventory summaries as hints, and can emit Docker/Ollama-volume scanner commands for traced blobs; `scripts/build_phase3_prompt_set.py` creates shared repeated prompt-set artifacts; `scripts/build_phase3_output_summary.py` creates saved-output templates with capture receipts; `scripts/phase3_trace_receipts.py` validates candidate trace receipts; `scripts/build_phase3_trace_receipt.py` creates fillable not-ready trace receipt templates; `scripts/build_phase3_runtime_capture_request.py` binds approval-bound runtime fills, the future live-proof fill, validators, and trace-receipt templates; `scripts/plan_phase3_runtime_capture_request.py` audits saved request path/status/approval/embedded-summary/source/validator-command drift and can emit a pre-capture operator handoff report with capture receipt requirements; `scripts/plan_phase3_dense_fallback_capture.py` exposes fill-output requirements for clearing fallback quality; `scripts/plan_phase3_policy_candidate_trace.py` exposes candidate-router-trace plus trace-receipt requirements and can write a Markdown handoff for clearing `no_replay_policy_candidate`; `scripts/plan_phase3_runtime_actuator_design.py` validates the selected backend actuator design boundary and explicit residency/control/cleanup gaps; `scripts/plan_phase3_runtime_actuator_spike.py` turns those gaps into proof requirements, dependency order, and completion gates for the next llama.cpp actuator spike; `scripts/plan_phase3_live_capability_proof.py` defines the validator and Markdown handoff for live residency observation/control, cleanup/restore proof, expected-context binding, existing source-bundle paths, and existing available-export artifact paths; `scripts/build_phase3_live_capability_proof_template.py` creates fillable proof templates for all six real bundles. PC Mixtral, Qwen3 Coder A3B, Qwen3 Coder 30B alias, Nemotron Cascade, Nemotron Nano A3B, and Gemma4 26B A4B now have repo-local scanner-derived inventories and valid trace/inventory pairing. | All real candidates still need a policy-candidate replay with a ready trace receipt, dense/full-runtime fallback comparisons, and a populated live capability proof artifact before Phase 3 handoff is ready. |
| Validate Phase 3 real-evidence bundle. | done | `memory-moe-mvp/data/phase3_real_evidence_bundle.fixture.json`, `scripts/build_phase3_real_evidence_bundle.py`, and `scripts/plan_phase3_real_evidence_bundle.py` build and validate handoff manifests with optional policy-candidate trace receipt, fallback, and live-proof artifact paths; attached live proof must match the consuming bundle context and prove existing source/export paths when ready, and invalid packet evidence is propagated as a bundle error; `scripts/plan_phase3_real_evidence_matrix.py` summarizes six repo-local valid-but-not-ready bundles, policy-candidate blocker counts, policy-candidate trace-receipt required/ready/blocked counts, no-reuse-distance and prompt-identity diagnostics, and no remaining scanner coverage gaps. | All current real bundles are valid but not ready: no replay policy candidate with a ready trace receipt, dense fallback comparison, live residency observation/control, and cleanup proof remain missing. |

## Phase 4: Backend Adapter Feasibility Spike

| Step | Status | Evidence | Blocker |
| --- | --- | --- | --- |
| Choose first backend substrate. | planned | Expected: comparison of llama.cpp patch, vLLM extension, hookable PyTorch adapter, MoE-Infinity-style adapter, and prototype runtime. | Needs Phase 1 and Phase 3 reports. |
| Identify routing visibility control point. | blocked | Expected: exact function, hook, API, or patch point that exposes routed expert ids before expert load. | Needs backend source inspection and/or fork spike. |
| Identify residency control point. | blocked | Expected: exact place to preload, pin, evict, demote, or load expert tensors. | Needs backend source inspection and/or fork spike. |
| Define cleanup and rollback. | planned | Expected: restore dense/full-residency behavior or prove no mutation occurred. | Needs selected control point. |
| Emit live-adapter artifact shape. | planned | Expected: before/after residency, policy decision, fallback, cleanup, and timing artifacts. | Needs selected adapter design. |

## Phase 5: Guarded Live Actuator Experiment

| Step | Status | Evidence | Blocker |
| --- | --- | --- | --- |
| Implement actuator behind a kill switch. | blocked | Expected: live mode can be disabled instantly. | Needs Phase 4 control point. |
| Run a tiny smoke model or tiny MoE first. | planned | Expected: successful preload/evict/pin/fallback cycle with cleanup. | Needs actuator implementation and test model. |
| Run one constrained real-model experiment. | planned | Expected: memory or latency effect compared with baseline. | Needs smoke success and approved hardware. |
| Compare against replay expectations. | planned | Expected: live result either matches replay assumptions or explains mismatch. | Needs live artifact bundle. |
| Decide whether to continue, fork, or stop. | planned | Expected: go/no-go decision with maintenance cost. | Needs live evidence. |

## Current Critical Blockers

1. Python hookable semantic routing still needs a compatible local/cloud
   Transformers MoE runtime.
2. Stock endpoints do not expose semantic expert routing.
3. PC Ollama itself is not instrumented with the hook yet, but local Ollama
   blobs are readable through direct patched llama.cpp.
4. Larger Nemotron-H GGUF targets may still need host-specific runtime and memory checks, but PC Cascade/Nano scanner inventory and trace replay are no longer blocked.
5. PC Llama 4 Scout and DeepSeek V3.1 full router traces need a higher-memory
   host or Docker memory increase.
6. Current real replay bundles have zero policy candidates because they do not
   yet show reuse-distance observations that justify managed loading.
7. No live runtime actuator exists.
8. Residency observation and residency control are missing.
9. Cleanup/restore proof is missing.
10. Processed expert-store work needs real checkpoint files and disk, but should
   not require loading the full model into RAM.

## Next Achievable Steps

1. Use the evidence packet blocker-resolution queue, recommended runtime-capture preflight, planned runtime-capture command contract, and selected/all-request runtime-command/manual execution coverage, starting with Mixtral if a single target is desired, to capture an approved candidate router trace, fill its trace receipt with existing source request, prompt-set, candidate-trace path bindings, explicit approval metadata, prompt-traffic acknowledgement, and per-event prompt identity metadata, then replay it for reuse-distance observations or document why the observed route pattern should stay no-go.
2. Export the planned launch-card template, then wire model-plane or launch-card runtime capture commands for the candidate trace and managed/dense output fills, or run those fills manually after explicit approval; then mark their capture receipts ready from the approved request, build the comparison artifact, and validate it.
3. Fill the live-capability proof template with approved residency observation/control and cleanup/restore evidence before Phase 4.
4. Package the llama.cpp Docker image and run command as a repeatable launch-card path.
5. Produce the backend observability comparison from captured artifacts.
6. Start the streaming write-path design only after real-model layout and disk budget are validated.
